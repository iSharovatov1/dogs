// import { render } from '@testing-library/react';

// import ShowCollage from '../components/modals/ShowCollage';

// test('renders learn react', () => {
//   const component = render(ShowCollage({ isOpen: true, handelClose: () => {} }));
//   expect(component).toMatchSnapshot();
// });
